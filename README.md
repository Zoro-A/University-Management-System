This currently has backend in Fast Apis you have to install fast api router uvicorn and pydantic to run the program the command to install all of these libraries is
pip install fastapi pydantic uvicorn
